name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_call:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  test:
    name: Lint, typecheck, test, build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint and format check (root)
        run: pnpm run lint

      - name: Typecheck (all packages)
        run: pnpm run typecheck

      - name: Test (all packages)
        run: pnpm run test

      - name: Build (all packages)
        run: pnpm run build

      - name: Verify @solana/idl dist artifacts exist
        run: |
          test -f packages/idl/dist/index.js
          test -f packages/idl/dist/index.cjs
          test -f packages/idl/dist/index.d.ts
          test -f packages/idl/dist/cli.js

      - name: Verify @solana/security-txt dist artifacts exist
        run: |
          test -f packages/security-txt/dist/index.js
          test -f packages/security-txt/dist/index.cjs
          test -f packages/security-txt/dist/index.d.ts
